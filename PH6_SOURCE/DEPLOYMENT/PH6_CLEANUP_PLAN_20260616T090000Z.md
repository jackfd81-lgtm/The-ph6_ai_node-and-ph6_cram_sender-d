# PH6 Cleanup Plan — Post-Audit Actions
**Generated:** 2026-06-16T09:00:00Z  
**Derived from:** PH6_DIAGNOSTIC_AUDIT_20260616T085500Z.md  
**Status:** PROPOSED — operator ratification required before any action  

V-01 (CLAUDE.md placement table) is already applied and closed.  
Items below are deferred; none are safety-blocking.

---

## ITEM 1 — BLUR_CAL_720p Path Comment (V-02)

**What:** `PH6_SOURCE/TESTS/USB_CAMERA_12000/pseudo_measure.py` line 46 references
`BLUR_CAL_720p.json 2026-06-01` without a full path. The file exists at
`ph6/cram_pu/validation_runs/BLUR_CAL_720p.json` (untracked evidence artifact).

**Why it matters:** Evidence-first rule — a calibration provenance comment that doesn't
resolve to a discoverable path weakens the audit chain. A future session cannot verify
the threshold without knowing where the calibration artifact lives.

**Risk:** LOW. The threshold value (0.3) is already canonical and operator-ratified. This
is a comment precision gap only; it does not affect runtime behavior.

**Blocking:** No.

**Proposed repair:**
```diff
-                            # calibration floor ~1.49 (BLUR_CAL_720p.json 2026-06-01)
+                            # calibration floor ~1.49 (ph6/cram_pu/validation_runs/BLUR_CAL_720p.json 2026-06-01)
```

**Operator decision needed:**
- [ ] Approve comment fix in `PH6_SOURCE/TESTS/USB_CAMERA_12000/pseudo_measure.py`
- [ ] Optionally: commit `BLUR_CAL_720p.json` to `PH6_SOURCE/DEPLOYMENT/` as a sealed
      calibration evidence artifact (not required for runtime correctness)

---

## ITEM 2 — Root DEPLOYMENT/ Ambiguity (V-04)

**What:** `/home/jack/DEPLOYMENT/` exists untracked at repo root. This is distinct from
the canonical `PH6_SOURCE/DEPLOYMENT/` (the operator-ratified report home). The root
directory contains: offline AI mock reports, a cluster SSH setup script, a hardware
expansion scope doc, fleet state, deployment checklist, and internal system test reports
from 2026-05-28.

**Current contents (as of audit):**
```
ph6_offline_ai_determinist_mock_20260614T*.report.txt  (3 files)
DEPLOYMENT_CHECKLIST.md
FLEET_STATE.md
jackjack_can_validation.sh
PH6_ARC_UPDATE_CHECK_20260614T111205Z.md
PH6_CLOSURE_NOTE_DESKTOP_DUALCAM_20260608T104926Z.md
ph6_cluster_ssh_setup.sh
ph6_fastpi_setup.sh
PH6_HARDWARE_EXPANSION_SCOPE_20260608.md
PH6_INTERNAL_SYSTEM_TEST_20260528T071022Z.md
PH6_INTERNAL_SYSTEM_TEST_20260528T073318Z.md
```

**Why it matters:** Canonical doctrine (CLAUDE.md): "Reports / session records →
`PH6_SOURCE/DEPLOYMENT/`". A second untracked DEPLOYMENT/ at root creates ambiguity
for AI sessions and operators about where artifacts live.

**Risk:** LOW. No authority content; purely organizational.

**Blocking:** No.

**Operator decision options (choose one):**
- [ ] **Option A — Merge:** Move contents into `PH6_SOURCE/DEPLOYMENT/` and delete root
      `DEPLOYMENT/`. Add `DEPLOYMENT/` to `.gitignore` to prevent recurrence.
- [ ] **Option B — Classify:** Keep root `DEPLOYMENT/` as a staging/scratch area and add
      it to `.gitignore` explicitly, with a note in CLAUDE.md that it is intentionally
      outside the canon tree.
- [ ] **Option C — Delete:** If contents are stale/superseded, delete after review.

---

## ITEM 3 — Untracked PH6_INTERNAL_SYSTEM_TEST Reports at Root (V-05)

**What:** 25+ files matching `PH6_INTERNAL_SYSTEM_TEST_*.md` exist at `/home/jack/`
(repo root), all untracked. These are generated by `ph6_internal_test.py` when run
directly. The most recent are from 2026-06-14. Some older ones are at
`PH6_SOURCE/DEPLOYMENT/` (correctly placed).

**Why it matters:** Root clutter; `git status` shows 25+ untracked entries making it
harder to spot meaningful new files. Canonical report home is `PH6_SOURCE/DEPLOYMENT/`.

**Risk:** LOW. Content is correct (PASS reports); location is wrong.

**Blocking:** No.

**Proposed repair:**
1. Determine where `ph6_internal_test.py` writes its report path. Currently it writes to
   a path relative to cwd or a default. Adjust the default output path to
   `PH6_SOURCE/DEPLOYMENT/` (or accept a `--out-dir` flag).
2. For the existing 25+ files: move to `PH6_SOURCE/DEPLOYMENT/` or delete if superseded.
3. Add `PH6_INTERNAL_SYSTEM_TEST_*.md` pattern to `.gitignore` at root if operator
   prefers to leave them at root as ephemeral scratch (Option B from Item 2 analogy).

**Operator decision needed:**
- [ ] Redirect test report output to `PH6_SOURCE/DEPLOYMENT/` in `ph6_internal_test.py`
- [ ] Or: add root pattern to `.gitignore` as intentional scratch
- [ ] Dispose of existing 25+ root-level reports (move or delete)

---

## ITEM 4 — OI-01: Hailo Hardware Run (STOP-SHIP BLOCKER)

**What:** Open Item OI-01 — Hailo hardware integration run + report. Listed as
STOP-SHIP in GAP_REGISTER_v3.0.md and CLAUDE.md OPEN ITEMS.

**Why it matters:** Production clearance is bounded to single-node + RSYNC export.
Hailo (hardware accelerator) integration is not cleared and blocks any expansion of the
production clearance scope to include hardware-accelerated inference.

**Risk:** STOP-SHIP for Hailo-dependent scope. Does not block current bounded single-node
CRAM operation (which is already production-cleared as of 2026-05-18).

**Blocking:** YES for Hailo-dependent scope expansion. NO for current bounded operation.

**What AI can do:**
- Prepare a Hailo integration test scaffold / checklist (advisory, Lane-2 only)
- Prepare the evidence receipt template
- Verify doctrine compatibility of any Hailo advisory path

**What only the operator can do:**
- Run the actual hardware — connect Hailo, execute test, capture real evidence
- Sign the integration report
- Close OI-01 in GAP_REGISTER_v3.0.md with a real artifact hash

**Next step:** Operator schedules hardware session. AI can prepare the test checklist
when requested. Do NOT close or mark as pending-close without a real hardware artifact.

---

## ITEM 5 — OI-03 / C02: Pi-to-Pi Live Transfer (STOP-SHIP BLOCKER)

**What:** Open Item OI-03 — real Pi-to-Pi live transfer log + replay proof. Also tracked
as Campaign C02. Listed as STOP-SHIP in GAP_REGISTER_v3.0.md and CLAUDE.md OPEN ITEMS.

**Why it matters:** Distributed authority path (Pi 5 → Pi 3B+ or Zero 2W) is not
proven. Any multi-node or distributed CRAM deployment is blocked until this evidence
exists.

**Risk:** STOP-SHIP for distributed deployment. Does not block current single-node
bounded operation (already production-cleared 2026-05-18).

**Blocking:** YES for distributed/multi-node scope. NO for current single-node operation.

**Prerequisites for hardware run:**
- Pi 3B+ (Scout-P) connected and reachable (currently TBD/no IP assigned)
- Transfer sender/receiver scripts present: `ph6/cram_pu/transfer_sender.py`,
  `ph6/cram_pu/transfer_receiver.py` — both exist in tree
- Network path verified between nodes
- Replay parity test ready: `run_two_pi_transfer_test.py` — exists in tree

**What AI can do:**
- Review transfer scripts for doctrine compliance (advisory)
- Prepare the evidence receipt template for C02
- Verify replay parity logic in `run_two_pi_transfer_test.py`

**What only the operator can do:**
- Bring Pi 3B+ online and assign it an IP
- Run the live transfer test
- Capture log + blake2b hash of the transfer receipt
- Close OI-03/C02 in GAP_REGISTER_v3.0.md with real artifact hash

**Next step:** Operator brings Pi 3B+ online. AI can review transfer scripts for
pre-run advisory check when requested.

---

## PRIORITIZATION (operator decision)

| # | Item | Effort | Blocking | Suggested Order |
|---|------|--------|----------|-----------------|
| 3 | Root test reports — .gitignore or move | Minutes | No | Do first (low effort, cleans status) |
| 2 | Root DEPLOYMENT/ ambiguity | Minutes | No | Do second (clarify canonical path) |
| 1 | BLUR_CAL_720p comment fix | Minutes | No | Do third (single-line diff) |
| 5 | OI-03 Pi-to-Pi hardware | Hours | YES (scope) | When Pi 3B+ is available |
| 4 | OI-01 Hailo hardware | Hours | YES (scope) | When Hailo hardware is available |

---

```json
{
  "proposed_by": "claude-code-lane2",
  "proposed_at_utc": "2026-06-16T09:00:00Z",
  "api_call_log_ref": "session-20260616T084556Z",
  "ratified_by": null
}
```

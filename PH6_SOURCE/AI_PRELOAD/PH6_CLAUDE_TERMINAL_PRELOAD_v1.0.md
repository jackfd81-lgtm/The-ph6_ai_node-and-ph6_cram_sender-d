# PH6 CLAUDE TERMINAL PRELOAD
## Constitutional Fleet Governance + Advisory AI Topology
## Classification: AUTHORITATIVE PRELOAD CONTEXT
## Priority: HIGH
## Drift Risk: CRITICAL IF IGNORED

---

# CORE PH6 RULES

PH6 is a deterministic scientific instrument.

Primary goals:

- deterministic replay
- crash-consistent preservation
- constitutional authority isolation
- auditability
- structural guarantees over policy guarantees

PH6 does NOT permit:
- probabilistic authority
- AI-issued PASS/DROP
- advisory authority escalation
- silent topology drift
- RSYNC blockage
- hidden schema mutation

---

# PRIMARY CONSTITUTIONAL PRINCIPLE

When a new guarantee is required:

```text
First ask:
can this be structural?

If yes:
make it structural.

Policy prohibition is used only when structural enforcement is genuinely impossible.
```

This principle governs:
- runtime
- hardware
- fleet topology
- authority segregation
- node design
- replay architecture
- advisory placement
- storage pathways

---

# PH6 FLEET TOPOLOGY

## Tier 0 -- Deterministic Authority

Node:
- Pi 3B+

Responsibilities:
- Lane 1 authority
- PSEUDO deterministic measurement
- PASS/DROP issuance
- CRAM-A authority path
- sequence issuance
- audit authority

This is the ONLY authority node.

---

## Tier 1 -- FAST CRAM Worker

Node:
- Healthy Pi 5 (ph6-fastpi)

Responsibilities:
- high-speed ingest
- FAST CRAM staging
- replay preparation
- export staging
- HOTSTORE operations

This node is NOT final authority.

---

## Tier 2 -- Advisory AI + CAN Telemetry

Node:
- Broken PCIe Pi 5 (jackjack)

Designation:

```text
PH6-L2-CAN-ADVISORY-01
```

Responsibilities:
- Claude/Klaw Terminal
- SoSo advisory mapping
- TOK topology analysis
- CAN telemetry ingestion
- CAN frame decoding
- replay interpretation
- operator advisory reports
- drift-risk analysis
- dashboarding
- telemetry synthesis

Important:

The broken PCIe lane is architecturally useful.

Because PCIe/NVMe authority storage is unavailable:
- authority storage is naturally segregated
- AI experimentation is naturally caged
- advisory compute remains separated from deterministic authority

CAN HAT operates over GPIO/SPI.
PCIe failure does not affect CAN telemetry functionality.

---

## Tier 0.5 -- Sentinel / Witness

Node:
- Pi Zero 2 W (ph6-zero-sentinel)

Responsibilities:
- heartbeat watch
- RSYNC monitoring
- witness timestamps
- backlog alerts
- node liveness monitoring
- optional DROP-only Smart Spigot

PASS authority is structurally absent.

There is no code path on this node capable of issuing PASS.

---

## Future Tier 2 Accelerator

Node:
- Jetson Nano 4GB

Responsibilities:
- future bounded acceleration
- advisory AI compute only

Important:
Jetson-class GPU accelerators have NO recertification path to authority.

They remain permanently advisory.

---

# EXECUTION ORDER

Correct PH6 order:

1. Capture / FAST CRAM
2. PSEUDO deterministic measurement
3. PSEUDO PASS/DROP authority
4. CRAM-A / CRAM-R handling
5. SoSo advisory mapping
6. TOK advisory topology
7. Claude/Klaw advisory reasoning
8. RSYNC export / witness verification

Claude/Klaw Terminal exists AFTER deterministic authority.

It interprets authority outputs.
It never generates authority outputs.

---

# STRUCTURAL AUTHORITY ABSENCE

The following capabilities are structurally absent from advisory nodes:

- PASS issuance
- CRAM-A authority writes
- sequence issuance
- audit-chain authority
- threshold governance
- deterministic adjudication
- replay sealing authority

These are not merely forbidden by policy.

There is no lawful architectural path allowing these actions.

---

# ALLOWED WRITE PATHS

Claude/Klaw output may ONLY write to:

```text
/var/ph6/mram-s/advisory/
/var/ph6/advisory_reports/
/var/ph6/can_advisory/
```

Forbidden destinations:

```text
/var/ph6/cram-a/
/var/ph6/cram-r/
/var/ph6/cram-0/
/var/ph6/audit/
/etc/ph6/
/var/ph6/sequence/
```

---

# IMPORTANT DESIGN PHILOSOPHY

PH6 is NOT:

- a distributed AI cluster
- a probabilistic autonomous system
- an AI-first architecture

PH6 IS:

```text
a distributed constitutional scientific instrument
```

Function may be distributed.

Authority remains singular.

---

# OPERATIONAL INTERPRETATION RULE

Claude/Klaw Terminal must behave as:

```text
an interpreter of authority
never the source of authority
```

Any recommendation produced by advisory systems:
- is non-authoritative
- may be ignored
- may not mutate deterministic logic
- may not bypass constitutional boundaries

---

# DRIFT PREVENTION RULES

Never:
- move AI upstream of PSEUDO
- allow advisory nodes to issue PASS
- allow GPU nodes into authority path
- allow CAN telemetry to alter thresholds automatically
- allow advisory write access into CRAM-A
- allow replay authority outside Tier 0

Any such change requires:
- explicit governance update
- committed canonical modification
- recertification review

---

# CURRENT ARCHITECTURAL STATUS

Current state:

```text
Authority:
  centralized

Function:
  distributed

AI:
  bounded

Replay:
  deterministic

RSYNC:
  protected

Advisory:
  constitutionally segregated
```

This topology is intentional.
Do not collapse these boundaries.
